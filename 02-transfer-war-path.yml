- name: Transfer WAR file path to deployment servers
  hosts: deploy
  become: yes
  tasks:
    - name: Copy WAR file path file from build server
      ansible.builtin.copy:
        src: /tmp/war_file_path.txt
        dest: /tmp/war_file_path.txt
